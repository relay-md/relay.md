{% extends 'layout.pug' %} {% block title %}{{ name }} {% endblock %}
{% block content %}
section.hero.is-link
 .hero-body
  .title
   figure.image.is-128x128
     img.is-rounded(src="{{user.profile_picture_url}}")
  .subtitle {{user.name | capitalize }} ({{user.username}})

.column
 section.section
  .title Account
  .subtitle Personal Information of your account
  table.table
   tr
    td
     span.icon
       i.fas.fa-envelope
    td {{user.email}}

 section.section
  .title API
  .subtitle Access Token
  {% for access_token in user.access_tokens %}
  div.icon-text
   span.icon
    i.fas.fa-key
   span
    tt
     strong {{access_token.token}}
   a(data-target="{{access_token.token}}" class="clipboard-this")
    span.icon
     i.fas.fa-copy
  div.mt-0
   small Last Used: &nbsp; {{access_token.latest_document_at }}
  {% endfor %}

{% endblock %}
